% Demonstrates that @startuml and @enduml are optional: when the diagram body
% does not contain them, PlantUML adds them automatically, so the environment
% may hold just the diagram. Compare with example-minimal.tex. See #4.
\documentclass{scrartcl}
\usepackage{plantuml}
\begin{document}
\begin{plantuml}
Alice -> Bob: test
\end{plantuml}
\end{document}
